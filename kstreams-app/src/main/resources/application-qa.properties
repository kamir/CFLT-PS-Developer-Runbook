# ==============================================================================
# QA Environment — Kafka Streams (Confluent Cloud)
# ==============================================================================
bootstrap.servers=${KAFKA_BOOTSTRAP_SERVERS}
security.protocol=SASL_SSL
sasl.mechanism=PLAIN
sasl.jaas.config=${KAFKA_SASL_JAAS_CONFIG}

schema.registry.url=${SCHEMA_REGISTRY_URL}
basic.auth.credentials.source=USER_INFO
schema.registry.basic.auth.user.info=${SCHEMA_REGISTRY_USER_INFO}

application.id=fraud-detection-app-qa
num.stream.threads=2
state.dir=/var/kafka-streams
commit.interval.ms=1000

# Exactly-once semantics for QA validation
processing.guarantee=exactly_once_v2
