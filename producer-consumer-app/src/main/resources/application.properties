# ==============================================================================
# Base Kafka Configuration — shared across all environments
# ==============================================================================
# Override per environment via application-{env}.properties or env vars.

client.id=payment-app
group.id=payment-consumer-group
